#include <bits/stdc++.h>
using namespace std;

int main()
{
    unordered_set<int> st;
    /*
    lower_bound and upper_bound functions does not works
    rest all functions are same as above
    it doesnot stored in any particular order it has a better comlexity than set in most cases
    except some when collision happens
    */
    return 0;
}